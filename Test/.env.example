# Copy this file to ".env" and fill in real values there — never in this
# example file, and never by pasting into a chat with anyone, AI or human.
#
#   cp .env.example .env
#   (then edit .env in your own editor)

OPENAI_API_KEY=sk-your-real-openai-key-here
OPENAI_MODEL=gpt-4o-mini

CF_AIGOV_URL=http://localhost:8787
CF_AIGOV_PUBLIC_KEY=pk-lf-from-ai-hub-sdk-projects
CF_AIGOV_SECRET_KEY=sk-lf-from-ai-hub-sdk-projects
