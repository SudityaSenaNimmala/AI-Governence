{
  "name": "@cloudfuze/ai-governance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "release": "node scripts/release.mjs",
    "dev:server": "npm --prefix server run dev",
    "dev:dashboard": "npm --prefix dashboard run dev",
    "scan": "npm --prefix agent run scan"
  }
}
